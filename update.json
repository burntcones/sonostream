{
  "versionCode": 28,
  "versionName": "2.3.6",
  "apkUrl": "https://github.com/burntcones/sonostream/releases/download/v2.3.6/aux.apk",
  "releaseNotes": "Fix random mid-track stops: auto-retry the same track once, then advance if Sonos stops again. Also recover from stale WiFi network binding (EPERM) that was silently breaking SOAP calls after WiFi events."
}
